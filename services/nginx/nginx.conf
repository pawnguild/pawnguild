upstream django_app {
    server web:8000;
}

server {
    listen 80;
    listen [::]:80;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # location / {
    #     return 301 https://pawntest.xyz$request_uri;
    #     #return 301 https://$host$request_uri;
    # }   

    location / {
        proxy_pass http://django_app;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}

# server {
#     listen 443 ssl;

#     ssl_certificate /etc/letsencrypt/live/pawntest.xyz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pawntest.xyz/privkey.pem;

#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

# 	location /static {
# 		alias /static;
# 	}

#     location / {
#         proxy_pass http://django_app;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host $host;
#         proxy_redirect off;
#     }
# }
